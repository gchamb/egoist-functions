{
  "name": "egoist-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "drizzle-kit": "^0.24.2",
    "drizzle-orm": "^0.33.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
